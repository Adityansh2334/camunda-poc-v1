spring.application.name=CAMUNDIAN

spring.mvc.static-path-pattern=/**

server.port= 8081

#Zeebe Configs {This configs will not work}
#camunda.client.region=
#camunda.client.cluster-id=
#camunda.client.auth.clientId=
#camunda.client.auth.clientSecret=

#zeebe.client.broker.gateway-address=localhost:26500
#zeebe.client.security.plaintext=true

zeebe.client.cloud.region=${ZEEBE_CLIENT_CLOUD_REGION}
zeebe.client.cloud.clusterId=${ZEEBE_CLIENT_CLOUD_CLUSTER_ID}
zeebe.client.cloud.clientId=${ZEEBE_CLIENT_CLOUD_CLIENT_ID}
zeebe.client.cloud.clientSecret=${ZEEBE_CLIENT_CLOUD_CLIENT_SECRET}
zeebe.client.requestTimeout=20s

# H2 Database configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

custom.search.key=
g.cse.cx=

#Custom Values
animal.image.size=large


# Camunda properties
#camunda.bpm.generate-unique-process-engine-name=true
#camunda.bpm.job-execution.enabled=true
